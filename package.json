{
    "name": "my-visualization-mod",
    "version": "1.0.0",
    "type": "module",
"scripts": {
  "postinstall": "chmod +x node_modules/.bin/mods-sdk",
  "build": "npx mods-sdk build",
  "build:dev": "npx mods-sdk build --watch --debug",
  "tsc:dev": "tsc --watch",
  "server": "npx mods-dev-server",
  "start": "npx mods-dev-server"
},
    "devDependencies": {
      "@spotfire/mods-api": "~1.3.0",
      "@spotfire/mods-dev-server": "^1.1.0",
      "@spotfire/mods-sdk": "^1.0.1",
      "esbuild": "^0.21.4",
      "prettier": "^3.3.0",
      "typescript": "^5.4.5"
    },
    "engines": {
      "node": ">=20.0.0"
    }
  }
  